<h4 class="text-center text-light mt-5" 
    style="font-size: {{ $size }}; font-weight: bold; background-color: {{ $bgColor }}; padding: 10px 20px; border-radius: 5px;">
    @if($icon)
        {{ $icon }}
    @endif
    {{ $title }}
    @if($icon)
        {{ $icon }}
    @endif
</h4>

